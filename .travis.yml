language: python
python:
  - "3.6"
  - "pypy3.5"

os:
  - linux

install:
  - pip install -r requirements.txt

script:
  - cd odmf
  - cp ../tests/conf-test-local.py conf.py
  - python start.py &
  # Timouts to let the server start up
  - sleep 5
# test via sqlitedb, mit guest user, jeweils alle "exposed" seiten
# TODO: postgres-scripts
